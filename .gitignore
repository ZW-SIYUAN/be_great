# ── Python ────────────────────────────────────────────────────
__pycache__/
**/__pycache__/
*.py[cod]
*.pyo
*.pyd

# ── Jupyter ───────────────────────────────────────────────────
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# ── Package build ─────────────────────────────────────────────
*.egg-info/
dist/
build/
*.egg

# ── IDE / OS ──────────────────────────────────────────────────
.idea/
.vscode/
.DS_Store
Thumbs.db

# ── Claude Code (local settings, not project config) ──────────
.claude/

# ── Model weights (large binary files) ───────────────────────
# .safetensors / .bin are model checkpoints; share via HuggingFace Hub instead
*.safetensors
*.bin
*.pt
*.pth

# ── Trainer checkpoints (可达数十GB，不入库) ──────────────────
trainer_great/
**/checkpoint-*/

# ── Root-level model directory generated during early experiments ──
electricity_great_model/

# ── Data / output files (不入库，体积大或可重新生成) ──────────
*.csv
*.png

# ── Legacy / scratch ──────────────────────────────────────────
testing/
